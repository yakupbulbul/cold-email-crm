"""
pytest.ini — project-wide pytest configuration.
"""
[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: marks tests as integration tests (require real services)
    unit: marks tests as fast unit/api tests (no external services required)
